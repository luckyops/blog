<% if (theme.analytics && theme.analytics.google){ %>
  <% if (theme.analytics.google.site_verification){ %>
    <meta name="google-site-verification" content="<%= theme.analytics.google.site_verification %>"/>
  <% } %>
  <% if (theme.analytics.google.gtag_id){ %>
    <script async src="https://www.googletagmanager.com/gtag/js?id=<%= theme.analytics.google.gtag_id %>"></script>
    <script>
      window.dataLayer = window.dataLayer || []
      function gtag() {
        dataLayer.push(arguments)
      }
      gtag('js', new Date())
      gtag('config', '<%= theme.analytics.google.gtag_id %>')
    </script>
  <% } %>
<% } %>

<% if (theme.analytics && theme.analytics.cloudflare){ %>
  <% if (theme.analytics.cloudflare.token){ %>
    <script defer src="https://static.cloudflareinsights.com/beacon.min.js" data-cf-beacon='{"token": "<%= theme.analytics.cloudflare.token %>"}'></script>
  <% } %>
<% } %>

<% if (theme.analytics && theme.analytics.bing){ %>
  <% if (theme.analytics.bing.site_verification){ %>
    <meta name="msvalidate.01" content="<%= theme.analytics.bing.site_verification %>"/>
  <% } %>
<% } %>


<% if (theme.analytics && theme.analytics.baidu){ %>
  <% if (theme.analytics.baidu.disable_transformation) { %>
    <meta http-equiv="Cache-Control" content="no-transform"/>
    <meta http-equiv="Cache-Control" content="no-siteapp"/>
  <% } %>
  <% if (theme.analytics.baidu.site_verification){ %>
    <meta name="baidu-site-verification" content="<%= theme.analytics.baidu.site_verification %>"/>
  <% } %>
  <% if (theme.analytics.baidu.id){ %>
    <script type="text/javascript">
      var _hmt = _hmt || []
      ;(function () {
        var hm = document.createElement('script')
        hm.src = 'https://hm.baidu.com/hm.js?<%= theme.analytics.baidu.id %>'
        var s = document.getElementsByTagName('script')[0]
        s.parentNode.insertBefore(hm, s)
      })()
    </script>
  <% } %>
<% } %>

<% if (theme.analytics && theme.analytics.umami){ %>
  <% if (theme.analytics.umami.id && theme.analytics.umami.host){ %>
    <script async defer data-website-id="<%= theme.analytics.umami.id %>" src="<%= theme.analytics.umami.host %>/umami.js"></script>
  <% } %>
<% } %>
